# Production Caddyfile for /etc/caddy/Caddyfile
# Auto-HTTPS via Let's Encrypt
# andre.dylanbochman.com redirects to echone.st (old domain)

echone.st {
	reverse_proxy localhost:5001 {
		flush_interval -1
	}
}

www.echone.st {
	redir https://echone.st{uri} permanent
}

andre.dylanbochman.com {
	redir https://echone.st{uri} permanent
}
